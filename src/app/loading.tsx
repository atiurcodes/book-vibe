import React from 'react';

const loading = () => {
    return (
        <div className='container mx-auto min-h-screen px-4 flex justify-center items-center'>
            <span className="loading loading-spinner text-secondary"></span>
        </div>
    );
};

export default loading;